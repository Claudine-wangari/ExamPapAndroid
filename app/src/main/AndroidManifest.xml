<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.exampap">
    <!-- Internet Permission -->
    <application
        android:allowBackup="true"
        android:icon="@drawable/strathimage"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TimetableActivity"></activity>
        <!-- Splash Screen -->
        <activity
            android:name=".SplashScreen"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_splash_screen"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Login Page -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/toolbar_content"></activity> <!-- Landing Page -->
        <activity
            android:name=".HomePage"
            android:label="@string/toolbar_content"
            android:theme="@style/AppTheme.NoActionBar"/> <!-- Exams Page -->
        <activity
            android:name=".ExamsActivity"
            android:label="@string/toolbar_content" />
    </application>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>